

export enum TypeOfQuestion {
  SINGLELINE,
  MULTILINE,
  INTEGER,
  YESNO
}


export enum SettingDarkMode {
  AUTO,
  DARK,
  LIGHT
}

export enum AccessStatus {
  PUBLIC,
  PRIVATE
}

export enum Language {
  ENG,
  JPN
}